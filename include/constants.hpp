#if !defined(CHRONIC_CONSTANTS_HEADER)
#define CHRONIC_CONSTANTS_HEADER
#pragma once

#define BGCOLOR(color)  (10 + (color))
#define FGCOLOR(color)  (20 + (color))

namespace chronic {
    namespace constants {
        extern const char *colon;
        extern const char *number[];
    }
}

#endif

